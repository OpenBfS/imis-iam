# Change this to https in productive environment
PROTOCOLL=http

# Configure base URL for Keycloak
KC_HOSTNAME=localhost
KC_HOSTNAME_PORT=48080

# Configure ServerName directive for Shibboleth SP in Apache httpd
# SP_HOSTNAME defaults to KC_HOSTNAME
SP_PORT=48081
