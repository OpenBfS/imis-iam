# Settings shared between containers

# These variables form the full URL of the client application in the form
# ${PROTOCOLL}://${HOSTNAME}:${PORT}${CLIENT_PATH}. All but the path also
# forms the public URL for Keycloak.
PROTOCOLL
HOSTNAME
PORT
CLIENT_PATH

CLIENT_SECRET
